# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
- name: Deploy to Firebase Hosting
  uses: FirebaseExtended/action-hosting-deploy@v0.1-alpha

on: [pull_request]

jobs:
  deploy_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # If your website has any build steps, run them here. For example:
      # - run: npm run build
      - uses: firebaseextended/action-hosting-deploy@v1
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PREPREPREVIEW }}"
          projectId: preprepreview
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels